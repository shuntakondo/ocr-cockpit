# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# env / secrets
.env
.env.*
!.env.example
*.pem

# local data (uploaded files, embedded postgres) — never commit
/storage/uploads/
/.pglite/
*.db

# tesseract.js language data cached at runtime
*.traineddata

# os / editor
.DS_Store
.vscode/*
!.vscode/extensions.json
*.log
npm-debug.log*

# generated sample files are committed (self-authored fakes); uploads are not
!/storage/samples/

# sales material (pitch, pricing, proposal copy) — local only, never publish
/sales/
